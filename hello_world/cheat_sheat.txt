Local docker commands:

docker compose build   --> image build


docker compose up      --> container start
docker compose up -d
docker logs a52bc2cf17f3 -f --tail 10

docker image rm ffd09fce1dc3   --> Delete specific image from image id

8aeac6b47e3d

241a66ca780b


local -> dev (image) -> beta (image2)


client dev(image)


GHCR Docker:

echo $ghcr_pat| docker login ghcr.io -u shreyaskn72 --password-stdin
docker login ghcr.io

docker build . -t ghcr.io/shreyaskn72/hello-world:latest
docker push -t ghcr.io/shreyaskn72/hello-world:latest
docker run -t ghcr.io/shreyaskn72/hello-world:latest
